version: "3.8"

services:
  searxng-proxy-search:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - CONFIG_PATH=/data/config.json
    restart: unless-stopped
